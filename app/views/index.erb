<!DOCTYPE html>
<html>
  <body>

    <h1>Recipe List</h1>
      <% @recipe.each do |recipe| %>
      <li>
        <% recipe_link = "/recipes/#{recipe.id}" %>
        <a href="<%= recipe_link %>"><%= recipe.name %></a>
      </li>
      <% end %>

  </body>
</html>
